name: CI

on:
  push:
    branches: [main]
  pull_request:
  merge_group:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  test-setup:
    name: "Test setup.sh (${{ matrix.distro }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - distro: ubuntu:24.04
          - distro: fedora:42
          - distro: archlinux:latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Test setup.sh in Docker
        run: |
          docker run --rm -v "$PWD:/src:ro" -w /tmp/test "${{ matrix.distro }}" \
            bash -c '
              cp -r /src/. . &&
              bash setup.sh --yes &&
              export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH" &&
              gcc --version &&
              buck2 --version &&
              cargo --version &&
              uv --version &&
              rg --version &&
              fd --version || fdfind --version
            '

  build:
    name: build seed
    runs-on: [self-hosted, large]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          clean: false
      - name: Install patchelf
        # Self-hosted runner skips ci-setup and blocks sudo via no_new_privs,
        # so install patchelf as a user binary in $HOME/.local/bin.
        # portabilize_run prefers system patchelf and falls back to a
        # SIGBUS-prone bootstrap if missing.
        run: |
          set -euo pipefail
          PATCHELF="$HOME/.local/bin/patchelf"
          if [ -x "$PATCHELF" ] && "$PATCHELF" --version >/dev/null 2>&1; then
            echo "patchelf already installed: $($PATCHELF --version)"
          else
            mkdir -p "$HOME/.local/bin"
            case "$(uname -m)" in
              x86_64)  ARCH=x86_64 ;;
              aarch64) ARCH=aarch64 ;;
              *) echo "::error::Unsupported arch $(uname -m)"; exit 1 ;;
            esac
            # The 0.18.0 release tarball has 'bin/patchelf' at top level.
            curl -fsSL "https://github.com/NixOS/patchelf/releases/download/0.18.0/patchelf-0.18.0-${ARCH}.tar.gz" \
              | tar -xzf - -C "$HOME/.local" ./bin/patchelf
            chmod +x "$PATCHELF"
            # Verify the binary actually runs — catches arch/abi/extract issues.
            if ! "$PATCHELF" --version; then
              echo "::error::patchelf at $PATCHELF failed to run after install"
              exit 1
            fi
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          # portabilize_run looks at /usr/bin and /usr/local/bin; symlink so
          # it finds patchelf without needing PATH lookup (Buck2 actions get
          # an empty PATH). Soft-fail since /usr/local/bin usually isn't
          # writable; portabilize_run's ~/.local/bin candidate covers it.
          if [ ! -e /usr/local/bin/patchelf ] && [ -w /usr/local/bin ]; then
            ln -sf "$PATCHELF" /usr/local/bin/patchelf 2>/dev/null || true
          fi
      - name: Install buck2
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL --retry 5 --retry-delay 3 --retry-all-errors \
            -o "$HOME/.local/bin/buck2.zst" \
            https://github.com/facebook/buck2/releases/download/2026-02-01/buck2-x86_64-unknown-linux-gnu.zst
          zstd -d -f -o "$HOME/.local/bin/buck2" "$HOME/.local/bin/buck2.zst"
          rm -f "$HOME/.local/bin/buck2.zst"
          chmod +x "$HOME/.local/bin/buck2"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Restart buck2 daemon
        run: buck2 kill 2>/dev/null || true
      - name: Resolve targets
        run: buck2 targets //...
      - name: Graph tests
        run: |
          failed=0
          for bxl in tests/graph/test_*.bxl; do
            name=$(basename "$bxl" .bxl)
            echo "::group::$name"
            buck2 bxl "//tests/graph/${name}.bxl:${name}" || failed=1
            echo "::endgroup::"
          done
          [ "$failed" -eq 0 ]
      - name: Build seed toolchain
        run: buck2 build //tc/bootstrap:seed-export --out seed-toolchain.tar.zst
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: seed-toolchain
          path: seed-toolchain.tar.zst
          compression-level: 0

  test:
    name: test
    needs: [build]
    runs-on: [self-hosted, large]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          clean: false
      - name: Install patchelf
        # Self-hosted runner skips ci-setup and blocks sudo via no_new_privs,
        # so install patchelf as a user binary in $HOME/.local/bin.
        # portabilize_run prefers system patchelf and falls back to a
        # SIGBUS-prone bootstrap if missing.
        run: |
          set -euo pipefail
          PATCHELF="$HOME/.local/bin/patchelf"
          if [ -x "$PATCHELF" ] && "$PATCHELF" --version >/dev/null 2>&1; then
            echo "patchelf already installed: $($PATCHELF --version)"
          else
            mkdir -p "$HOME/.local/bin"
            case "$(uname -m)" in
              x86_64)  ARCH=x86_64 ;;
              aarch64) ARCH=aarch64 ;;
              *) echo "::error::Unsupported arch $(uname -m)"; exit 1 ;;
            esac
            # The 0.18.0 release tarball has 'bin/patchelf' at top level.
            curl -fsSL "https://github.com/NixOS/patchelf/releases/download/0.18.0/patchelf-0.18.0-${ARCH}.tar.gz" \
              | tar -xzf - -C "$HOME/.local" ./bin/patchelf
            chmod +x "$PATCHELF"
            # Verify the binary actually runs — catches arch/abi/extract issues.
            if ! "$PATCHELF" --version; then
              echo "::error::patchelf at $PATCHELF failed to run after install"
              exit 1
            fi
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          # portabilize_run looks at /usr/bin and /usr/local/bin; symlink so
          # it finds patchelf without needing PATH lookup (Buck2 actions get
          # an empty PATH). Soft-fail since /usr/local/bin usually isn't
          # writable; portabilize_run's ~/.local/bin candidate covers it.
          if [ ! -e /usr/local/bin/patchelf ] && [ -w /usr/local/bin ]; then
            ln -sf "$PATCHELF" /usr/local/bin/patchelf 2>/dev/null || true
          fi
      - name: Install buck2
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL --retry 5 --retry-delay 3 --retry-all-errors \
            -o "$HOME/.local/bin/buck2.zst" \
            https://github.com/facebook/buck2/releases/download/2026-02-01/buck2-x86_64-unknown-linux-gnu.zst
          zstd -d -f -o "$HOME/.local/bin/buck2" "$HOME/.local/bin/buck2.zst"
          rm -f "$HOME/.local/bin/buck2.zst"
          chmod +x "$HOME/.local/bin/buck2"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Reset buck2 state
        run: |
          buck2 kill 2>/dev/null || true
          rm -rf buck-out
      - name: Unit tests
        run: |
          targets=$(buck2 uquery 'kind(sh_test, //tests:) - set(//tests:test-seed-isolation //tests:test-kde-iso-boot) - attrfilter(labels, heavy, kind(sh_test, //tests:))' 2>/dev/null)
          buck2 test $targets
      - name: Hermeticity gate (base rootfs ELF dependency closure)
        run: buck2 test //tests:test-hermeticity --target-platforms //platforms:linux-target-bare
      - name: Build systemd container rootfs
        run: |
          buck2 build //packages/linux/system:systemd-container-rootfs --out systemd-container-rootfs.tar
          xz -9f systemd-container-rootfs.tar
      - name: Build minimal ISO
        run: buck2 build //packages/linux/system:buckos-minimal-iso --out buckos-minimal.iso
      - name: Build KDE ISO
        run: buck2 build //packages/linux/system:buckos-live-installer-kde --out buckos-kde.iso
      - name: KDE ISO boot test (QEMU)
        # Direct-boots the live kernel + initramfs under KVM and checks the
        # kernel reaches init and systemd starts as PID 1 in the initrd.
        # Self-skips (exit 0) when /dev/kvm is unavailable on the runner.
        run: buck2 test //tests:test-kde-iso-boot
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: systemd-container-rootfs
          path: systemd-container-rootfs.tar.xz
          compression-level: 0
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: buckos-minimal-iso
          path: buckos-minimal.iso
          compression-level: 0
      - if: github.event_name == 'merge_group'
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: buckos-kde-iso
          path: buckos-kde.iso
          compression-level: 0

  test-aarch64:
    name: build & test (aarch64)
    runs-on: [self-hosted, ARM64]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          clean: false
      - name: Apply local config
        run: |
          cat > .buckconfig.local << 'EOF'
          [cell_aliases]
            ovr_config = prelude
            fbsource = buckos
            fbcode = buckos
          EOF
      - name: Install patchelf
        # Self-hosted ARM64 runner skips ci-setup, so install patchelf
        # directly. portabilize_run prefers system patchelf to avoid the
        # buckos-patchelf bootstrap that SIGBUSes when seed sysroot ABI
        # doesn't match what buckos patchelf was originally linked against.
        run: command -v patchelf >/dev/null || sudo apt-get install -y --no-install-recommends patchelf
      - name: Install buck2
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL --retry 5 --retry-delay 3 --retry-all-errors \
            -o "$HOME/.local/bin/buck2.zst" \
            https://github.com/facebook/buck2/releases/download/2026-02-01/buck2-aarch64-unknown-linux-gnu.zst
          zstd -d -f -o "$HOME/.local/bin/buck2" "$HOME/.local/bin/buck2.zst"
          rm -f "$HOME/.local/bin/buck2.zst"
          chmod +x "$HOME/.local/bin/buck2"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Restart buck2 daemon
        run: buck2 kill 2>/dev/null || true
      - name: Resolve targets
        run: buck2 targets //...
      - name: Graph tests
        run: |
          failed=0
          for bxl in tests/graph/test_*.bxl; do
            name=$(basename "$bxl" .bxl)
            echo "::group::$name"
            buck2 bxl "//tests/graph/${name}.bxl:${name}" || failed=1
            echo "::endgroup::"
          done
          [ "$failed" -eq 0 ]
      # Unit tests require a seed toolchain (host-tools-exec).
      # No aarch64 seed is built yet — skip until one is available.

  test-seed:
    name: test with seed
    needs: [build]
    runs-on: [self-hosted, mini]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          clean: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: seed-toolchain
          path: .
      - name: Install patchelf
        # Self-hosted runner skips ci-setup and blocks sudo via no_new_privs,
        # so install patchelf as a user binary in $HOME/.local/bin.
        # portabilize_run prefers system patchelf and falls back to a
        # SIGBUS-prone bootstrap if missing.
        run: |
          set -euo pipefail
          PATCHELF="$HOME/.local/bin/patchelf"
          if [ -x "$PATCHELF" ] && "$PATCHELF" --version >/dev/null 2>&1; then
            echo "patchelf already installed: $($PATCHELF --version)"
          else
            mkdir -p "$HOME/.local/bin"
            case "$(uname -m)" in
              x86_64)  ARCH=x86_64 ;;
              aarch64) ARCH=aarch64 ;;
              *) echo "::error::Unsupported arch $(uname -m)"; exit 1 ;;
            esac
            # The 0.18.0 release tarball has 'bin/patchelf' at top level.
            curl -fsSL "https://github.com/NixOS/patchelf/releases/download/0.18.0/patchelf-0.18.0-${ARCH}.tar.gz" \
              | tar -xzf - -C "$HOME/.local" ./bin/patchelf
            chmod +x "$PATCHELF"
            # Verify the binary actually runs — catches arch/abi/extract issues.
            if ! "$PATCHELF" --version; then
              echo "::error::patchelf at $PATCHELF failed to run after install"
              exit 1
            fi
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          # portabilize_run looks at /usr/bin and /usr/local/bin; symlink so
          # it finds patchelf without needing PATH lookup (Buck2 actions get
          # an empty PATH). Soft-fail since /usr/local/bin usually isn't
          # writable; portabilize_run's ~/.local/bin candidate covers it.
          if [ ! -e /usr/local/bin/patchelf ] && [ -w /usr/local/bin ]; then
            ln -sf "$PATCHELF" /usr/local/bin/patchelf 2>/dev/null || true
          fi
      - name: Install buck2
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL --retry 5 --retry-delay 3 --retry-all-errors \
            -o "$HOME/.local/bin/buck2.zst" \
            https://github.com/facebook/buck2/releases/download/2026-02-01/buck2-x86_64-unknown-linux-gnu.zst
          zstd -d -f -o "$HOME/.local/bin/buck2" "$HOME/.local/bin/buck2.zst"
          rm -f "$HOME/.local/bin/buck2.zst"
          chmod +x "$HOME/.local/bin/buck2"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Configure seed
        run: printf '[buckos]\nseed_path = seed-toolchain.tar.zst\n' >> .buckconfig.local
      - name: Verify seed archive
        run: |
          echo "Archive size: $(stat -c%s seed-toolchain.tar.zst) bytes"
          zstd -dc seed-toolchain.tar.zst | tar tf - | head -20
          zstd -dc seed-toolchain.tar.zst | tar tf - | grep -c host-tools || echo "WARNING: no host-tools in archive"
      - name: Tests
        run: |
          targets=$(buck2 uquery 'kind(sh_test, //tests:) - set(//tests:test-seed-isolation //tests:test-kde-iso-boot) - attrfilter(labels, heavy, kind(sh_test, //tests:))' 2>/dev/null)
          buck2 test $targets

  required-checks:
    name: required checks
    needs: [test-setup, build, test, test-aarch64, test-seed]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: |
          results=(
            "${{ needs.test-setup.result }}"
            "${{ needs.build.result }}"
            "${{ needs.test.result }}"
            "${{ needs.test-aarch64.result }}"
            "${{ needs.test-seed.result }}"
          )
          for r in "${results[@]}"; do
            if [[ "$r" != "success" ]]; then
              echo "::error::One or more required jobs failed or were cancelled"
              exit 1
            fi
          done

  release:
    needs: [required-checks]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: seed-toolchain
          path: seed-toolchain
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: buckos-minimal-iso
          path: buckos-minimal-iso
      - uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2.6.1
        with:
          tag_name: dev
          name: Development Build
          prerelease: true
          make_latest: false
          files: |
            seed-toolchain/seed-toolchain.tar.zst
            buckos-minimal-iso/buckos-minimal.iso
